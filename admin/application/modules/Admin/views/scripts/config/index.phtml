<?php
defined ('_VALID_MOS') or
    die('Direct Access to this location is not allowed');
?>
<div id="cpanel"><?php if($this->menuItems !== NULL) {
        foreach($this->menuItems as $menuItem) {
            $this->quickIcon( $menuItem['link'], $menuItem['icon'], JText::_( $menuItem['text'] ) );
        }
    }
?></div>